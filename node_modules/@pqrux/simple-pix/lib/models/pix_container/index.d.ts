import PIXInfo from "../pix_info";
export default class PIXContainer {
    id: string;
    children: {
        [id: string]: PIXInfo | PIXContainer;
    };
    constructor(id: string);
    addChild(child: PIXInfo | PIXContainer): void;
    toString(withoutLeadingInfo?: boolean): string;
}
