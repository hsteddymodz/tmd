"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const lengther_1 = __importDefault(require("../../utils/lengther"));
class PIXContainer {
    constructor(id) {
        this.children = {};
        this.id = id;
    }
    addChild(child) {
        this.children[child.id] = child;
    }
    toString(withoutLeadingInfo = false) {
        const serialized = Object.keys(this.children)
            .sort((a, b) => parseInt(a) - parseInt(b))
            .map(id => this.children[id].toString())
            .join("");
        if (!withoutLeadingInfo) {
            return `${this.id}${(0, lengther_1.default)(serialized)}${serialized}`;
        }
        return serialized;
    }
}
exports.default = PIXContainer;
