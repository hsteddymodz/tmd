"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const lengther_1 = __importDefault(require("../../utils/lengther"));
class PIXInfo {
    get data() {
        return this._data;
    }
    set data(data) {
        this._data = data.length >= this.maxLength ? data.substring(0, this.maxLength) : data;
    }
    constructor(id, data, maxLength) {
        this.id = id;
        this.maxLength = maxLength;
        this.data = data;
    }
    toString() {
        return `${this.id}${(0, lengther_1.default)(this.data)}${this.data}`;
    }
}
exports.default = PIXInfo;
