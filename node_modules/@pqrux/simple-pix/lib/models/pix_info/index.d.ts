export default class PIXInfo {
    id: string;
    private _data;
    maxLength?: number;
    get data(): string;
    set data(data: string);
    constructor(id: string, data: string, maxLength?: number);
    toString(): string;
}
